{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run in Playdate Simulator",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "Build Playdate Game",
      "program": "${userHome}/Developer/PlaydateSDK/bin/Playdate Simulator.app",
      "args": ["game.pdx"],
      "cwd": "${workspaceFolder}"
    }
  ]
}